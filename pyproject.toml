[project]
name = "s3-duckdb-pipeline"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dbt-core>=1.11.8",
    "dlt>=1.25.0",
]

[tool.uv.workspace]
members = [
    "my_duckdb_pipeline",
]